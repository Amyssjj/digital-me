{
  "name": "@digital-me/runtime-claude-code",
  "version": "0.1.0-pre",
  "description": "Claude Code runtime adapter for the digital-me brain: hooks wired into 3 Claude Code events (UserPromptSubmit / Stop / PreToolUse) — the scripts live in the shared @digital-me/agent-hooks package — the `digital-me` skill, and a settings.json template.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./skills/*": "./skills/*",
    "./templates/*": "./templates/*"
  },
  "files": [
    "dist",
    "src",
    "skills",
    "templates",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@digital-me/agent-hooks": "workspace:*",
    "@digital-me/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^5.0.1",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=22.5"
  }
}
