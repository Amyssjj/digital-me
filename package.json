{
  "name": "digital-me-os",
  "version": "0.1.0-pre",
  "private": true,
  "description": "Digital Me OS \u2014 one brain (digital-me-brain, served by brain-host) shared by every agent runtime: knowledge, taste, operations, runtime adapters.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "packageManager": "pnpm@11.1.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r --if-present test:coverage",
    "lint": "eslint .",
    "knip": "knip",
    "dm": "node packages/cli/dist/bin/digital-me.js",
    "build:cli-bundle": "node scripts/build-cli-bundle.mjs",
    "typecheck": "pnpm -r typecheck",
    "sanitize:check": "bash scripts/sanitize-check.sh",
    "regression": "bash scripts/regression-suite.sh",
    "ci": "pnpm sanitize:check && pnpm build && pnpm typecheck && pnpm lint && pnpm knip && pnpm test:coverage",
    "dashboard": "pnpm --filter @digital-me/dashboard dev",
    "prepare": "[ -d .git ] && command -v husky >/dev/null && husky install || true"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "knip": "^6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0"
  }
}
